# Git LFS Configuration for Docsify Godot Embed Project

# ===========================================
# SELECTIVE LFS TRACKING FOR GITHUB PAGES COMPATIBILITY
# ===========================================
# Only track WASM and PCK files with LFS to avoid GitHub Pages serving issues
# All other files (including index.js with Engine class) are committed directly
# CI will fetch LFS files and include them in the deployment

# WASM files (WebAssembly binaries) - Use LFS for large binaries
*.wasm filter=lfs diff=lfs merge=lfs -text

# PCK files (Godot packed data) - Use LFS for potentially large game assets
*.pck filter=lfs diff=lfs merge=lfs -text

# ===========================================
# TEXT FILES (Normal Git Tracking)
# ===========================================
# Ensure critical files use normal Git tracking for GitHub Pages compatibility

*.md text=auto eol=lf
*.txt text=auto eol=lf
*.json text=auto eol=lf
*.js text=auto eol=lf
*.css text=auto eol=lf
*.html text=auto eol=lf
*.xml text=auto eol=lf
*.yml text=auto eol=lf
*.yaml text=auto eol=lf
*.sh text=auto eol=lf
*.gd text=auto eol=lf

# Configuration files
*.cfg text=auto eol=lf
*.ini text=auto eol=lf

# Godot project files
*.import -text
*.tscn -text
*.tres -text
*.godot -text

# ===========================================
# SPECIAL CASES
# ===========================================
# Ensure docsify plugin is directly accessible
docsify-embed-godot.js text=auto eol=lf
